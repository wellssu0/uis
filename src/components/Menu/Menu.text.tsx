import React from "react"

import { render } from '@testing-library/react'
import Menu from './Menu'

describe('test Menu component', ()=>{
  const element = render(<Menu/>)
  it('should render correct menu component', ()=>{
    
  })
})